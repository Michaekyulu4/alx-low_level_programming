#include "main.h"
#include <stdio.h>

/**
 * swap_int - Swaps value of integers
 * @a: value to be swaped
 * @b: value to be swaped
 */
void swap_int(int *a, int *b)
{
	int a;

	m = *a;
	*a = *b;
	*b = m;
}
