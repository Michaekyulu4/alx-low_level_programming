#include "main.h"
#include <stdio.h>

/**
 * mul - Miltiple 2 numbers
 * @a: the value to be computed
 * @b: the value to be computed
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
